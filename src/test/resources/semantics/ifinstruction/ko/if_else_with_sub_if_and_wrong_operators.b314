/* Test If else playPlus */

#import "world.map"

    x as integer;
    y as integer;
    z as char[5];
    t1 as boolean[5];

main as function():void
do
   x := 5;
   y := 10;

   if ((true and false) or false) then
         left(1);
         right(2);
    else
       left();
       x := x + 2;
    end
   if ((false or (x + 5) )  + true) then
         right(4);
         x := x / 2;
    else
        right(1);
        z[0] := x * 2;
        Z[1] := z[1];
    end


dig();

end