/* Test If else playPlus */

#import "world.map"

    x, y as integer;
    z as boolean;


main as function():void
do
   x := 0;
   y := 0;

   if (x = 7) then

      x := x + 1;
      y := y + 1;
    else

      x := x + 10;
      y := z + 10;

  end
   if ((false or (x + 5)) and true) then
         right(4);
         x := x / 2;
    else
        right(1);
        x := x * 2;
    end

dig();

end