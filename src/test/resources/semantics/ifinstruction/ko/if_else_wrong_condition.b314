/* Test if else playPlus */

#import "world.map"

    x as integer;
    y as integer;
    z as char[5];
    t1 as boolean[5];


main as function():void
do
   x := 10;
   y := 10;
   z[0] := 'a';
   z[1] := 'b';
   z[2] := 'c';
   z[3] := 'd';
   z[4] := 'e';

   if ((i + 2) > 2) then
         y := z[0] + 1;
         dig();
    else
       left();
       x := x / 2;
       up(2);
       left(3);
    end
   if (false or (x > 5))  then
         right(4);
         x := z[1] / 2;
    else
        right(1);
        x := x * 2;
    end


dig();

end