/* Test playPlus */

 #import "world.map"

     x  as integer;
     t4 as integer[42];
     t5 as integer[42, 42];
     t6 as integer[42, 42];

main as function():void
do
  x := 0;
  x := (x + 1) - (48 + 2);
  x :=  x + ((x / 2) * (x - 3));
  x := (1 - x) % (5 - 2);
  x := x - t4[0];
  x := x * t5[0, x];
  x := x % (t6[0, t5[0, t4[0]]] + 2);
  x := x % (t6[0, t4[0]] * 34 + 6);
  x := x % (t6[0, t5[1, t4[0]]] + 2);

dig();

end